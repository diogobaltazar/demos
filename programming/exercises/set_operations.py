# is subset
list(set(l1) <= set(l2))

# is superset
list(set(l1) >= set(l2))

# intersection
list(set(l1) & set(l2))

# union
list(set(l1) | set(l2))

# difference
list(set(l1) - set(l2))

# symmetric difference
list(set(l1) ^ set(l2))